---
# ESLint v9 flat config (root)
# Each app/package extends this or adds their own rules

# This file intentionally left minimal — each workspace configures its own eslint.config.mjs
# Shared rules live in packages/eslint-config (to be added in Faza 1)
